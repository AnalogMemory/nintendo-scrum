import ScrollTrigger from 'scrolltrigger-classes'

(function () {
  document.addEventListener('DOMContentLoaded', function () {
    const trigger = new ScrollTrigger({ // eslint-disable-line no-unused-vars
      centerVertical: true
    })
  })
})()
